# NODE_ENV: Specifies the environment in which the application is running (e.g., development, production).
# PORT: The port number on which the server will listen.
# DATABASE_URL: Connection string for the MongoDB database.
# IMAGEKIT_PUBLIC_KEY: Public API key for ImageKit integration.
# IMAGEKIT_PRIVATE_KEY: Private API key for ImageKit integration.
# IMAGEKIT_URL_ENDPOINT: Base URL endpoint for ImageKit resources.
# JWT_SECRET: Secret key used for signing JWT tokens.

# Storage Configuration
# VIDEO_STORAGE_TYPE: Storage service for videos (local, s3, imagekit)
# THUMBNAIL_STORAGE_TYPE: Storage service for thumbnails (local, s3, imagekit)
# AWS_REGION: AWS region for S3 storage
# AWS_ACCESS_KEY_ID: AWS access key for S3 storage
# AWS_SECRET_ACCESS_KEY: AWS secret key for S3 storage
# AWS_BUCKET_NAME: AWS S3 bucket name
